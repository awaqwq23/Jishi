# 生产部署结果与沙箱修复

用户已明确回复“确认执行此方案”，据此执行生产更新。没有再次要求部署授权。

## 首次执行与恢复

首次执行已批准的 `/tmp/jishi-deploy-f02effc.sh`，完成完整备份及加密还原比较后启动 API 时失败。日志定位到 `uv_interface_addresses returned Unknown system error 97`：systemd 的 RestrictAddressFamilies 未允许 Node/Wrangler 查询本机网卡所需的 AF_NETLINK。

自动回滚已执行，原代码、数据库、秘密文件、Nginx/systemd 配置和更新清单恢复，三个服务 active，公网健康返回 200。未清理失败现场或任何旧备份。首次暂存只测试了部分沙箱限制，是本次遗漏；已在 AGENTS.md 加入完整限制组合运行测试要求。

修复提交 `7ac3043` 已推送 GitHub：API/Web 均保留 AF_UNIX、AF_INET、AF_INET6 并增加 AF_NETLINK，不放开公网监听或服务文件权限。

## 重新验证及部署

- 从干净提交建立独立 `/opt/jishi-stage-7ac3043`，保留相同依赖版本。Linux 全项目检查、更新契约再次通过；初次复制遗漏 workspace 内嵌依赖导致的类型检查失败，在补齐相同已验证依赖后消除。
- 使用完整生产单元限制组合、独立 `/var/lib/jishi-stage-7ac3043`、端口 8877/3300 测试 API 迁移、Web ExecStartPost 和健康检查。两服务 active、NRestarts=0，随后停止测试服务。
- 执行 `/tmp/jishi-deploy-7ac3043.sh`，先创建新的部署前完整加密备份，再同步并启动。沿用已独立保存在本机的备份密钥，使用新的随机 IV。
- 约 2026-09-09 07:10 UTC（北京时间 15:10）完成生产基本验证，脚本退出 0。

## 已验证结果

- `jishi-api`、`jishi-web`、`nginx` active，API/Web NRestarts=0。
- 内网 API/Web 与公网 `/note/`、`/note/health`、子域名健康检查成功。
- 无效登录返回 JSON 401；0.4.3/0.4.4 UA 分别验证主页/健康 200，未登录 bootstrap 和错误密码登录 JSON 401。
- 伪造身份头无法认证（JSON 401）；不受信任 Origin 的写入被拒绝（JSON 403）。
- 新限流表存在；公网清单与源码构建一致：`web-e79ca056cdea7451`。Windows 0.4.5 / Android 0.4.6 安装包维持不变。
- 8 张业务表部署前后记录数及完整内容摘要完全一致：2 users、2 auth_credentials、8 categories、6 reminder_presets、20 todos、7 schedule_items、3 schedule_records、8 diary_entries。未打印账号信息、密码摘要或业务内容。
- 生产秘密文件 600、服务账号所有；数据根目录 700；备份密钥和加密备份 600、root 所有。
- 07:10 UTC 之后旧客户端日志中没有新增非预期 4xx/5xx；探测产生的 401 属于明确的无效认证测试。检查时尚无真实成功登录/bootstrap 请求，不把探测当作真实用户登录验证。

## 备份、密钥和清理状态

本次成功部署前的完整回滚备份：

`/var/backups/jishi/pre-7ac3043-20260909-security/production.tar.aesgcm`

备份覆盖旧代码、数据库、媒体/下载区、秘密配置、三个 Nginx 配置和两个 systemd 单元。加密后已解密并逐字节比较，再删除新备份中的临时明文 tar。密钥在服务器 `/etc/jishi/backup-20260909.key`，独立本机副本在仓库外 `C:/Users/82028/.ssh/jishi-backup-20260909.key`，未上传 GitHub。不得丢失唯一可用密钥副本。

已有账号实际登录及数据展示仍待用户复核，已通过异步问题请求。依据发布门禁，复核前未删除旧备份、首次回滚恢复目录或远端暂存内容。候选清理项需在全部验证通过后逐项重新枚举和检查：

- 旧备份 `/var/backups/jishi/pre-df68e52-20260908T021500Z`。
- 首次失败备份和私有恢复现场 `/var/backups/jishi/pre-f02effc-20260909-security`。
- `/tmp/jishi-stage-f02effc`、`/tmp/jishi-stage-f02effc-db`、本次两个 `/tmp/jishi-release-*.tar` 和验证脚本/配置；仅列出并检查准确路径后删除。
- `/opt/jishi-stage-7ac3043`、`/var/lib/jishi-stage-7ac3043`、`/var/www/jishi-stage-downloads-7ac3043` 及 `/run/systemd/system/jishi-stage-*-7ac3043.service` 为本次明确命名的独立测试资源，测试服务已停止。

最终只保留本次 `pre-7ac3043-20260909-security` 前一版本备份；没有把“待清理”写作“已清理”。在线 SQLite/媒体仍由服务器以明文读取，本次实现的是访问保护和备份加密，不是端到端加密。

## 鸿蒙状态

用户已确认开发者实名认证和应用创建完成。0.3.1 的 unsigned HAP/APP 已构建；发布证书、Profile、真机验证及 AppGallery 上传仍未完成，浏览器连接之前返回 Debugger unattached。本次生产部署确认不代表鸿蒙已经上架。
