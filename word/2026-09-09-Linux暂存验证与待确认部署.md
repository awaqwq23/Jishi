# Linux 暂存验证与待确认部署

用户已确认华为开发者账号完成实名认证并创建应用。浏览器能枚举具体 AppGallery Connect 应用页面，但读取仍超时并返回 `Debugger unattached`。本地无发布签名配置、P12/PFX/CER/CSR/P7B 文件，已请求重新连接浏览器及补充应用包名、公开联系邮箱；没有重复要求实名认证。

## 生产只读检查

`服务器设置.md` 已保存为 157 字节，继续被 Git 和发布归档排除。使用既有部署密钥成功连接 `awaqwq233@104.214.169.232`，未使用或输出文件中的密码。

- `jishi-api`、`jishi-web`、`nginx` 均 active；API/Web 仅监听 127.0.0.1，公网监听 22/80/443。
- 剩余磁盘约 18GB，842MB RAM、2GB swap。未修改防火墙、SSH 认证或其他应用。
- `.dev.vars` 非符号链接、服务账号所有、600，AUTH_SECRET 长度有效；只检查条件，未输出值。
- 允许来源当前仅旧 nip.io 域名，部署必须补齐两个正式域名；TRUST_PROXY_HEADERS 当前未设置，合并的 Nginx 配置覆盖 X-Real-IP 后启用。
- 数据目录 750、SQLite 644，计划收紧到目录 700/文件 600。
- 生产 Certbot 段及同机 heartbeat 配置已保留，只在对应 Jishi API 位置加入身份头清理和 JSON 429/413。

## 暂存结果

源提交 `f02effc`，独立目录 `/tmp/jishi-stage-f02effc`，数据库 `/tmp/jishi-stage-f02effc-db`，发布包 `/tmp/jishi-release-f02effc.tar`。安装依赖后，实际执行以下验证通过：

- Linux `npm run check`：Server TypeScript / dry-run / 6 安全测试，Web lint / 构建 / 3 测试，Windows 4 测试，备份加密测试及更新契约。
- 全新数据库迁移 0000/0001/0002 全链通过，没有访问在线持久化目录。
- Windows 与 Linux 更新哈希一致：`web-e79ca056cdea7451`。
- `node_modules/.bin/wrangler` 和 `vinext` 实际存在、可执行。
- Linux audit 无高危或严重项；仍有 4 中危开发依赖问题。
- 合并 Nginx 配置使用独立主配置 `nginx-test.conf` 通过 `nginx -t`；systemd 单元通过语法检查。
- 暂存 API/Web 在独立端口 8877/3300，以 ProtectSystem=strict、ProtectHome、NoNewPrivileges、0077 umask 实际启动并返回 HTTP 200；测试服务已停止。原生产服务保持 active。

## 待执行的准确命令

尚未执行下列生产命令。脚本已上传并通过 `bash -n`，本地和远端 SHA256 一致：`15e7b82843fc483735ced8e6227af123a54a34020a2c22d3eb6054ba4a307ee7`。完整脚本位于 `server/deploy/native/releases/deploy-f02effc-20260909.sh`。

```powershell
scp -p -i C:/Users/82028/.ssh/jishi_deploy_20260907_ed25519 C:/Users/82028/.ssh/jishi-backup-20260909.key awaqwq233@104.214.169.232:/home/awaqwq233/.jishi-backup-20260909.key
ssh -i C:/Users/82028/.ssh/jishi_deploy_20260907_ed25519 awaqwq233@104.214.169.232 "chmod 600 /home/awaqwq233/.jishi-backup-20260909.key && sudo -n bash /tmp/jishi-deploy-f02effc.sh"
```

脚本先复查路径、秘密文件及生产 Nginx 哈希；安装独立备份密钥到 `/etc/jishi/backup-20260909.key`。本机仓库外保留同一密钥副本，未上传 GitHub。

停止 Jishi API/Web 后创建 `/var/backups/jishi/pre-f02effc-20260909-security/production.tar.aesgcm`，覆盖部署前完整代码、数据库、下载区、秘密配置、三个 Nginx 配置及两个 systemd 单元。加密备份先做解密还原比较，再删除临时明文 tar。

预计 Jishi 短暂停机约 2–5 分钟，取决于备份和服务启动时间；Nginx 与 heartbeat 不停机。同步显式保留 `.dev.vars*`、`.env*`，数据库不参与代码同步。补充允许来源，收紧数据权限，安装已验证配置，先 API/迁移再 Web，最后验证并 reload Nginx。

发生失败时自动停止新服务，从本次加密备份恢复原代码、数据库、秘密文件、配置与清单并启动旧服务；失败恢复目录保留供检查。脚本执行公网健康、无效登录 JSON 401、更新标识和迁移表检查。

已有账号登录/bootstrap、旧客户端日志差异仍需部署后验证；在全部通过前不会清理旧备份或暂存目录。通过后仅保留本次完整前一版本备份，并按 AGENTS.md 枚举和检查准确路径后清理。

状态：等待用户对上述准确命令、停机、备份和回滚范围作最终明确确认；尚未更新生产。
