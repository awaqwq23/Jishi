# 安装包下载与更新修复记录

## 用户要求

用户反馈：客户端会显示新版本提示，但点击后无法下载安装包并完成更新，要求修复并更新服务器。

## 只读诊断

- 生产 `jishi-api`、`jishi-web`、`nginx` 均为 `active`。
- 根域名 `/note/updates/latest.json` 与独立 `jishi` 域名 `/updates/latest.json` 均返回 HTTP 200。
- Windows 0.4.5 与 Android 0.4.6 安装包在两个入口均返回 HTTP 200，Content-Length、服务器 SHA256、本地安装包及清单完全一致。
- 生产秘密文件通过存在、非符号链接、属主、600 权限和 `AUTH_SECRET` 非空门禁，未读取或输出秘密值。
- 最近 30000 条 Nginx 访问记录中没有客户端安装包 GET；故障发生在更新按钮点击后、网络下载请求发出前。

## 修复

- 更新按钮改为真实的同源下载链接，不再使用整页 `window.location.assign` 导航。
- 根据当前运行路径自动选择 `/downloads/` 或 `/note/downloads/`，不再强制从清单中的另一个域名下载。
- Nginx 下载目录增加 `Content-Disposition: attachment`，让 Electron、Android WebView 与浏览器明确进入文件下载流程。
- 保持 Windows 0.4.5、Android 0.4.6 版本和既有 API/清单字段不变；通过新的 Web `releaseId` 让已安装 0.4.x 客户端刷新兼容修复。

## 发布状态

- 新 Web 发布号：`web-4ac12598051f51c5`；稳定源码哈希：`4ac12598051f51c583c07212a3cf37328e214eba34a1e0c4cfe67585246f7594`。
- `npm run check`：通过。包含 Server TypeScript、Wrangler dry-run、6 项安全测试、Web lint/生产构建/4 项测试、Windows 4 项兼容测试、备份加密测试和更新契约检查。
- `npm audit --audit-level=high`：通过，高危/严重漏洞为 0；剩余 4 项为既有 Drizzle Kit/esbuild 开发链中危问题。
- 在全新临时 D1 目录依次应用 `0000`、`0001`、`0002` 迁移全部成功，并验证用户、待办、定期任务、日记和限流表存在；临时目录已删除。
- 功能提交：`682c7cd40dd2d5e41331c406246982fe5d95a3b8`（`fix(updates): restore installer downloads`），仅包含 7 个任务文件；已推送 `origin/main` 并用 `git ls-remote` 核验远端哈希。
- Linux 独立暂存：从该 GitHub 提交建立 `/opt/jishi-stage-682c7cd`，使用 Node 24.19.0 / npm 11.17.0 完成 `npm ci`，确认根目录 Wrangler/Vinext 可执行；Linux 全项目检查、高危审计、全新迁移链和更新契约均通过。
- Windows 与 Linux 的发布号、稳定 Web 哈希一致，均为 `web-4ac12598051f51c5` / `4ac12598051f51c583c07212a3cf37328e214eba34a1e0c4cfe67585246f7594`。
- 从三份生产 Nginx 站点配置的只读副本生成仅增加附件响应头的暂存配置，保留 Certbot 管理段；组合后的完整 Nginx 配置通过语法检查。
- 部署脚本 `server/deploy/native/releases/deploy-682c7cd-20260914.sh` 已通过本地/远端 `bash -n` 和只读预检；上传副本 SHA256 为 `B0688A5C9DC77BD527C2815E3A041C223668F750761CB2B10647424BFB04B8AA`。
- 部署准备提交：`a9a17ff470ff5e8a62fab8ae210f782dd45c1bf2`（`docs(deploy): prepare guarded installer fix rollout`），已推送 `origin/main` 并核验远端哈希。

## 生产部署结果

- 用户在看到准确命令、备份、短暂停机、回滚及清理范围后明确回复“确认执行以上生产命令”。
- 执行 `sudo -n bash /opt/jishi-stage-682c7cd/deploy-update-downloads-682c7cd.sh` 成功，未触发自动回滚。
- 新建并验证完整加密备份 `/var/backups/jishi/pre-682c7cd-20260914-updates`；加密包 SHA256 校验通过。生产秘密文件在同步前后均通过存在、非符号链接、属主、600 权限及 `AUTH_SECRET` 长度门禁，未输出秘密值。
- 同步显式保护 `.dev.vars`、`.dev.vars.*`、`.env`、`.env.*`、数据库、媒体和现有安装包；三份实际生产 Nginx 配置仅增加附件响应头，未覆盖 Certbot 管理段。
- `jishi-api`、`jishi-web`、`nginx` 均为 `active`，完整 Nginx 配置复测通过；内部 API/Web、根域名 `/note/`、独立 `jishi` 域名及两端健康接口均返回 HTTP 200。
- 公网清单已发布 `web-4ac12598051f51c5`，稳定 Web 哈希与部署源码一致；生产 `npm run check:updates` 通过。
- 无效登录返回 JSON 401；使用已有账户记录构造的短时签名会话读取 bootstrap 成功，未输出账户标识或会话值；所需迁移表仍全部存在。
- Windows/Android 安装包在根域名与独立域名均返回正确 Content-Length 和 `Content-Disposition: attachment`；真实 Range GET 返回 HTTP 206 且读取 1 字节。
- 使用 `JishiWindows/0.4.3`、`JishiWindows/0.4.4`、`JishiAndroid/0.4.4` 进行清单、健康和下载探针，部署窗口内没有新增 4xx/5xx；API/Web 无 error 级日志。
- 全部验证通过后删除旧备份 `pre-7ac3043-20260909-security`、`pre-df68e52-20260908T021500Z`、`pre-f02effc-20260909-security`，只保留本次部署前备份；删除本次 `/opt/jishi-stage-682c7cd` 与 `/var/lib/jishi-stage-682c7cd` 暂存目录。
- 独立复核确认生产 `UpdatePrompt.tsx` SHA256 与本地一致，三份 Nginx 配置哈希与已验证副本一致，每份仅有一个附件响应头，备份校验和通过且暂存目录不存在。

自动化已覆盖更新按钮生成同源链接、服务器附件响应和真实字节下载；用户现有设备上的最终人工点击安装不由自动化替代。
