# 鸿蒙签名与应用市场发布门禁（2026-09-16）

## 用户要求与当前边界

用户要求给鸿蒙 HAP 配置发布签名并发布到应用市场。此前项目记录指出用户已完成华为开发者实名认证和应用创建，但本次无法实时读取 AppGallery Connect：浏览器连接两次返回 `nodeRepl.fetch request failed`。仓库 `clients/harmony/build-profile.json5` 的 `signingConfigs` 仍为空；工程内没有发布 `.p12/.cer/.p7b`。当前 `outputs/harmony/Jishi-Harmony-0.3.1-unsigned.hap` 经 HarmonyOS SDK `hap-sign-tool.jar verify-app` 报“signature not found”，不是可上架包。本次没有取得发布密钥、Profile 或应用市场审核权限，因此**没有完成签名、真机验证、上传、提交审核或上架**。

## 已完成的本地准备

- 将本地发布签名配置 `clients/harmony/signing.local.json5` 排除 Git；构建脚本也支持仓库外 `-SigningProfilePath`，不会输出配置内容。
- release 构建没有本地签名配置时直接失败；即使产出 `.app`，也必须抽出其中所有 HAP 并使用 SDK 验签工具验证后才能复制到 `outputs/harmony/`。签名配置所在 ASCII 暂存目录在结束时清理，避免留下加密签名密码。
- README 补齐开发者本人创建 P12/CSR、申请发布 CER/Profile、代理提醒能力获批后重生 Profile、DevEco 手动签名、签名 HAP 真机验证及签名 APP 上架流程。

## 核验与待办

- PowerShell 脚本语法分析无错误；`git diff --check` 通过；Git 忽略规则已验证。
- 无签名配置的 release 前置检查按预期拒绝；debug HAP 重新构建成功，仍显示 `No signingConfig found`，所以该 HAP 不能安装或上架。
- 使用旧 unsigned 配置测试 release APP：Hvigor `assembleApp` 成功，但脚本检测到 unsigned 包并拒绝交付；本次带配置的 ASCII 暂存目录 `C:\Users\82028\AppData\Local\Temp\jishi-harmony-3f98157e20694d32b5441ab72c32d248` 退出后已确认不存在，相应 `outputs/harmony/` 交付目录也不存在。发布 Profile、证书和 CSR 文件类型已排除 Git。
- `npm run check:updates` 通过；`npm audit --audit-level=high` 返回 0，报告 4 个 moderate、无 high/critical。本次没有正式发布，且没有以强制破坏性升级处理非高危开发依赖。
- AppGallery Connect 需确认 `cn.jishi.todo` 的应用记录、代理提醒申请状态、发布证书/Profile及商店资料；当前无法在线核验。华为代理提醒能力的发布 Profile 必须与授权一致，获批后重新生成并采用手动签名。
- 用户提供了 Developer ID、验证公钥以及项目级 Client ID/Client Secret/API Key。这些不是 HAP 发布签名材料，不能替代 `.p12/.cer/.p7b`。Client Secret/API Key 已出现在聊天中，未写入仓库或命令，必须在 AppGallery Connect 轮换后再用于其他集成。本机没有 DevEco IDE 可执行程序，新增 `generate-release-key.ps1`，使用 DevEco JBR 的 `keytool` 在 Git 忽略目录交互生成 P12/CSR，密码不经过脚本参数或聊天。
- 当前 Web/Server 代码仅找到“退出当前账号”，未找到公开隐私政策入口或注销账号流程；应用市场所需真实隐私政策、联系邮箱、真实截图、分级及账号注销安排不能凭空补写或宣称已具备。开发者须确认资料后才能提交。
- 真实 HarmonyOS 设备需要测试主/备站、登录、权限允许/拒绝、杀进程后到点通知及换账号提醒隔离。自动构建无法代替这些验收。

官方依据：[代理提醒开放能力](https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/agent-powered-reminder)、[工程签名配置字段](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/ide-hvigor-build-profile-V5)、[AppGallery 发布流程](https://developer.huawei.com/consumer/cn/appgallery/devstart/)。

## 版本、提交与部署

本次只修改发布构建门禁、Git 忽略规则、签名说明和本记录；没有修改 HarmonyOS 原生运行逻辑或重新发布安装包，保持未上架的 0.3.1 / 3000001。Git 提交标题计划为 `fix(harmony): guard unsigned release packages`，实际提交与远端状态以 Git 记录为准。未触碰生产 Web/Server、数据库、更新清单或生产秘密文件，因此没有生产 SSH 部署，也没有生产备份或停机；AppGallery 发布仍为待完成，不能计作部署成功。
