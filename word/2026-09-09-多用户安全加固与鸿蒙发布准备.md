# 多用户安全加固与鸿蒙发布准备

## 用户要求与范围

保护 `服务器设置.md`，完善鸿蒙并争取上架，检查多用户传输及服务端数据安全，完成后推送 GitHub 和更新生产服务。生产变更仍执行 AGENTS.md 的 Linux 暂存、备份、准确命令展示及最终确认门禁。

## 已实现

- 本地服务器连接文件加入 Git ignore、Git archive export-ignore 和 Docker 构建上下文排除；证书私钥、鸿蒙缓存及本机构建配置也排除。未读取或输出秘密值。
- 默认关闭无可信网关密钥的身份请求头认证，保留原 Cookie 名称、签名和旧客户端 API。校验会话有效期，避免无效/缺失过期时间通过认证。
- 校验分类和提醒预设归属；导出关联表同时匹配用户，阻止历史伪造引用泄漏其他账号名称。
- 新增 0002 迁移及 SQLite 持久化认证限流，账号/IP 使用摘要；跨源写入限制、真实请求体大小限制、JSON 错误与内部错误脱敏。
- 用户媒体禁止共享缓存；导入替换改为一个原子事务，错误时保留原分区。
- 新增 AES-256-GCM 流式备份加解密工具，拒绝篡改、错误密钥及覆盖文件。此工具尚未用于生产备份，在线数据库不因此变为加密数据库。
- 准备 Nginx 上游证书验证、身份头清理、JSON 限流与 systemd 权限保护模板；修复严格 umask 下更新清单必须仍可由 Nginx 读取的问题。模板尚未合并到生产配置。
- 鸿蒙升级 0.3.1 / 3000001；受限 HTTPS 桥接、后台代理提醒、加载反馈、返回操作、禁用文件访问；Web 退出或进入登录页清理原账号提醒，权限不足时显示降级提示。
- 修复 ArkTS 桥接对象类型，构建脚本通过 ASCII 临时目录兼容中文仓库路径，支持 HAP / release APP；明确未签名包不可安装或上架。

## 验证

- `npm run check` 全部通过：Server TypeScript + Wrangler dry-run、6 项隔离/认证/错误/事务安全测试、Web lint / 生产构建 / 3 项测试、Windows 4 项主机兼容测试、1 项加密备份综合测试、更新契约。
- 在独立全新临时数据库完整执行 0000、0001、0002，全部成功；未访问生产数据库。
- `npm audit --audit-level=high` 通过，0 高危/严重，仍有 4 项 Drizzle 开发工具链相关中危依赖问题。
- HarmonyOS 26 SDK + Hvigor 6.26.4 已完成 ArkTS 编译和 debug unsigned HAP；发布包最终结果见下方补充记录。没有可用签名凭据，未进行真机测试。
- `git diff --check` 与服务器设置文件忽略规则检查通过。Web 缓存生成目录已加入 lint 排除，源码仍参与 lint。

## 尚未完成与生产前提

- 本次多次检查 `服务器设置.md` 均为 0 字节（最后修改时间 2026-09-09 08:44:20），已请求用户保存编辑器内容。因此没有 SSH 连接、线上安全审计、Linux 暂存、备份或生产修改；不能宣称生产已安全或服务端已更新。
- 部署前必须确认 ALLOWED_ORIGINS 覆盖主/备用 origin，API 仅监听回环地址，Nginx 覆盖可信来源 IP 后才启用 TRUST_PROXY_HEADERS。默认不信任任意公网 IP 请求头。
- 活跃 SQLite 和媒体仍由服务端以明文读取。需要实机检查文件权限、端口、TLS、备份保管和磁盘保护；没有实现端到端加密。保留现有 PBKDF2 密码格式以兼容旧账号。
- 现有客户端“保存密码”仍依赖本地浏览器存储；本次仅在退出时清除，并未改为系统密钥库。该客户端风险需要独立兼容迁移，不能将服务器审计视作全端无漏洞证明。
- Huawei 浏览器连接报 Debugger unattached，开发者实名认证、现有应用、发布证书及 PUBLISH_AGENT_REMINDER 授权尚未核实。需公开联系邮箱、隐私政策、账号注销途径、真实截图、真机验证后才能完成 AppGallery 上传和最终提交。
- 生产最终确认不是目前的阻塞原因：目前先缺少已保存的连接信息。只有完成暂存、只读检查及可审阅的精确生产方案后，才向用户请求最终部署确认。

没有生产备份路径或回滚路径可记录，因为本次尚未执行生产操作；未清理任何旧生产备份。

## 鸿蒙发布构建结果

`build-hap.ps1 -BuildMode release -Artifact app` 实际执行成功（44 个任务）。完整 SDK 为 26.0.0.105，目标 26.0.0，最低兼容 5.0.0(12)。包内 `module.json` 确认包名 `cn.jishi.todo`、版本 0.3.1、构建号 3000001；归档 CRC 检查通过且包含编译后的 `ets/modules.abc`。

本地交付物（均未签名，不提交 Git，也不能直接提交审核）：

- `outputs/harmony/Jishi-Harmony-0.3.1-unsigned.app`：436295 字节，SHA256 `B909D1AFEB4931C07DA70D7E03CC83BA513A5B52F28756190D6C1CC9F9F16DD5`。
- `outputs/harmony/Jishi-Harmony-0.3.1-unsigned.hap`：444415 字节，SHA256 `2F0F117D1734E38A12C4CF5430578C5A4B1ACC7C6D6ADD6F953B024ABF927E5B`。

Web 更新标识 `web-e79ca056cdea7451`，Windows 0.4.5 / Android 0.4.6 壳版本维持不变。Linux 哈希一致性核验仍待 SSH 暂存环境。
