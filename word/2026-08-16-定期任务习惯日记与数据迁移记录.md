# 记时模块扩展与数据迁移记录

- 记录日期：2026-08-16
- 涉及范围：Web、Windows/Android 远程页面、Server API、数据库

## 用户原始发言

> 1、给设置里的自定义时间加上单位，最好可以有时分秒三个单位可以设置，而不是只能输入数字
> 2、卡片透明度调整后仍然无法生效，请修复
> 3、添加定期任务、习惯、日记三个新模快
> 定期任务如同系统闹钟，规定单次、每天、每周、选定周几、节假日等，书写计划，并显示已完成和未完成任务（也是左滑完成右滑回收站）
> 习惯和定期任务差不多，不过不用设置具体时间，规定单次、每天、每周、选定周几、节假日等，书写计划，并显示做到和未做到的习惯（也是左滑完成右滑回收站）
> 日记可以记录当天的日记并自动保存、也可以查看和修改以前的日记
>
> 最后、应该能以文件的方式（比如json或数据库文件）分别导出、导入账户配置、待办、定期任务、习惯、日记，方便玩家转移和保存、最好也支持导出为md文件
>
> 修复以上内容，并且把我的bug要求、以及你的修复，在文件夹word，以md文档形式记录我的发言和你修复的内容。然后把server端推送到服务器

## 完成的修复与功能

### 1. 自定义提醒时间单位

- 每一个提前提醒时间都改为“数值 + 单位”的独立输入。
- 单位可选择小时、分钟或秒，每个提醒组最多 8 个时间点。
- 原有提醒数据仍按分钟兼容保存；秒会转换为分钟小数，旧数据无需迁移。
- 通知检查频率提高到 1 秒，使按秒设置的提醒能够正常触发。

### 2. 卡片透明度

- 修正卡片背景颜色写法，让透明度变量真正作用于待办、定期任务和日记卡片。
- 降低自定义背景上方原本过厚的页面遮罩，避免卡片与页面底色接近而看不出透明变化。
- 设置页新增带纹理的即时预览，拖动时直接展示效果，松开、键盘调整结束或失焦时才保存一次。
- 服务端统一把透明度限制在 15% 至 100%，避免非法数值造成界面异常。

### 3. 定期任务

- 新增独立的“定期任务”模块和数据库表。
- 支持单次、每天、每周、选定周几、节假日/周末等重复方式，并支持开始日期、计划说明和精确到秒的执行时间。
- 按选中日期分别显示未完成、已完成和回收站内容。
- 左滑标记完成或重新打开，右滑移入回收站；回收站支持恢复和永久删除。
- 页面运行且本设备提醒已开启时，会在定期任务设定时间发送通知。

### 4. 习惯

- 新增独立的“习惯”模块，共用可靠的周期计划与完成记录模型，但不要求具体时间。
- 支持单次、每天、每周、选定周几、节假日/周末等规则。
- 按日期分别显示未做到、已做到和回收站内容。
- 左滑记录做到或改为未做到，右滑移入回收站；回收站支持恢复和永久删除。

### 5. 日记

- 新增独立的“日记”模块，每个账号每天保存一篇日记。
- 默认打开当天，可通过日期或历史列表查看和修改以前的日记。
- 停止输入 0.8 秒后自动保存到服务器；切换日期时会刷新尚未发送的最后内容。
- 自动保存加入修订号与请求竞态保护，避免较早的保存结果覆盖继续输入的新内容。

### 6. 分模块导入与导出

- 设置页新增“数据导入与导出”，可分别处理账户配置、待办、定期任务、习惯和日记。
- 每个模块可导出 JSON 备份，也可导出便于阅读和归档的 Markdown 文档。
- JSON 文件包含格式版本、模块标识和导出时间；导入时校验格式与模块是否匹配。
- 导入只替换当前账号对应模块的数据，不会影响其他账号；操作前会再次确认。
- 账户配置备份包含昵称、界面设置、分类和提醒组，不包含密码、登录凭据或会话。
- 待办导入按分类名和提醒组名重新关联，定期任务与习惯导入会重新建立完成记录关系。

## 数据库与接口

- 新增 `schedule_items`：定期任务和习惯计划。
- 新增 `schedule_records`：按日期保存完成/做到记录。
- 新增 `diary_entries`：按日期保存日记。
- 新增计划增删改、完成记录、日记自动保存、分模块 JSON/Markdown 导出及 JSON 导入接口。
- 新增增量数据库迁移 `0001_modules_and_portability.sql`，不会删除现有待办和账户数据。

## 规则说明

- 当前“节假日/周末”按周六、周日执行。中国法定节假日及调休需要独立年度日历数据，界面中已明确显示当前规则，避免把周末规则误认为官方调休日历。
- JSON 导入属于“替换对应模块”，建议导入前先导出该模块的 JSON 备份。

## 本地验证

- Web ESLint：通过。
- Web 生产构建：通过。
- Web 自动回归测试：3/3 通过。
- Server TypeScript 类型检查：通过。
- Windows 外壳测试：4/4 通过。
- 更新清单、安装包哈希与客户端标记一致性检查：通过。
- `npm audit --audit-level=high`：通过；没有高危或严重漏洞，现有 4 项均为开发工具链的中危依赖告警。
- 数据库增量迁移：已通过内存 SQLite 完整执行 `0000 → 0001`，三个新表均建立成功；生产启动阶段还会再次核验迁移状态。
- 生产部署：已完成，功能提交为 `8ba5a01 feat(planner): add recurring plans and portable data`。

## 生产部署结果

- 部署时间：2026-08-16 21:53（Asia/Shanghai）。
- 部署前已在服务器隔离目录完成 Linux 数据库迁移、Server dry-run、Web lint、生产构建及 3/3 回归测试。
- 生产部署前自动备份了原代码、数据库和更新清单，备份目录为 `/var/backups/jishi/before-8ba5a01-20260816T135158Z`，大小约 1.9GB。
- `jishi-api`、`jishi-web`、`nginx` 均为 `active`。
- 服务器内部 API 健康检查正常，Web 页面标题为“记时 · 待办”。
- 生产数据库已直接查询确认存在 `schedule_items`、`schedule_records`、`diary_entries` 三张新表。
- 公网主页和 `https://awaqwq233.com/note/health` 均返回 HTTP 200。
- 公网更新清单已发布 Web 标识 `web-f9a10b9e90907224`，Windows 与 Android 安装包版本继续保持 0.4.4。

## 登录故障与恢复

- 部署后 Windows 0.4.4 客户端重新登录时显示“操作没有完成，请稍后重试”。
- Nginx 访问日志显示 `/api/auth/login` 连续返回 HTTP 500；`jishi-api` 原始堆栈显示缺少符合要求的 `AUTH_SECRET`。
- 根因是部署同步使用 `rsync --delete` 时，没有排除被 Git 忽略的生产专用 `/opt/jishi/server/.dev.vars`，导致该文件被删除。
- 已得到用户确认后，从部署前备份中只恢复 `.dev.vars`，所有者恢复为服务账号、权限恢复为 600，并重启 `jishi-api`。
- 恢复后 API 健康检查正常；服务器内部无效账号登录正确返回 JSON 401，公网登录代理链也返回 HTTP 401 和 `application/json`，不再返回 500。

## 后续 AI 与旧版本兼容约束

- 新增仓库根目录 `AGENTS.md`，新 AI 会话进入项目后应自动读取。
- 新增全局 Codex 技能 `jishi-release-guard`，位置为 `C:\Users\82028\.codex\skills\jishi-release-guard\SKILL.md`，并已通过技能结构校验。
- 规则明确要求兼容仍在使用的 Windows/Android 0.4.3、0.4.4 和 Web/PWA 缓存版本；API 与数据库变更必须增量兼容。
- 所有后续生产同步必须保护 `.dev.vars`、`.env*`、数据库、媒体和更新清单；API 重启前必须验证秘密文件存在、权限正确和必需键非空，但禁止输出密钥值。
- 部署门禁新增旧客户端 User-Agent 4xx/5xx 检查，以及“无效登录必须返回 JSON 401”的契约测试。

## 追加需求：提醒与透明外观修复

### 用户原始发言

> 1、自定义提醒时间应该是天、小时、分钟，而不是小时，分钟，秒
> 2、“添加一个提醒时间”有时候点击没反应，请修复
> 3、卡片透明度和亚克力效果不能在选择后及时更新。同时也新增背景透明度、亚克力设计
> 4、如果卡片透明拖动的话会同时显示回收站和完成这一整块
>
> 确保每个新ai都能在修改和创建项目前阅读skill和约束、约束加上每次更新都要推送到远端、只保留前一个版本的备份并删除多余文件

### 本次修复

- 自定义提醒单位改为“天、小时、分钟”；原有分钟字段和 API 保持不变，以增量方式兼容 0.4.3/0.4.4 客户端。历史秒级小数在新版界面编辑时归整为至少 1 分钟。
- 修复“添加一个提醒时间”偶发无反应：不再固定追加可能被去重的 10 分钟，而是自动选择尚未使用的默认时间，确保每次点击都新增一行（最多 8 行）。
- 卡片透明度拖动时直接更新全局设置状态，松开、触控结束、键盘调整结束或失焦后才同步服务器；卡片亚克力开关改为先即时预览再同步，失败会提示并回载服务器配置。
- 卡片透明度和卡片亚克力同时应用于待办、定期任务与习惯卡片。
- 新增背景透明度与背景亚克力开关，均即时预览并同步到账号配置；服务端以可忽略的新字段保存，旧客户端不受影响。
- 滑动操作背景改为按实际拖动距离只展开当前方向：右滑仅显示“移入回收站”，左滑仅显示“完成/做到”，透明卡片下不再同时透出两整块操作层。
- Web 更新清单保持 Windows/Android 0.4.4 安装包版本不变，仅生成新的 Web `releaseId` 与源码哈希，以便已安装客户端刷新远程页面。

### AI 与远端保留规则补强

- 根目录 `AGENTS.md` 和全局 `jishi-release-guard` 技能现在互相要求：新 AI 在任何项目检查、修改、创建、打包或部署前，必须完整读取两者，并在首次进度说明中确认。
- 每次完成的更新必须提交任务相关文件；存在 Git remote 时推送 Git，未配置 Git remote 时必须明确说明，并在用户最终确认后通过 SSH 部署生产远端，不能把仅本地提交描述为已推送。
- 每次生产成功部署后只保留本次部署前创建的一个前版本备份。只有新版本全部验证通过后，才可逐一验证并删除更早备份；失败或回滚期间禁止清理。
- 成功验证后仅清理明确列出的旧暂存包、解压目录和可重建残留；数据库、媒体、秘密文件、更新清单、当前发布文件和唯一回滚备份永久排除在清理范围外。
- 更新后的技能已通过 `quick_validate.py` 校验。

### 本地验证

- Web ESLint：通过。
- Web 生产构建与自动回归测试：通过，3/3。
- Server TypeScript 类型检查：通过。
- 浏览器本地应用外壳可加载；由于本地未配置登录/API 数据，账号内设置页未使用生产账号做交互模拟。
- 生产 Linux 暂存验证、提交、部署与线上复测：等待部署门禁完成后补充。
